[project]
name = "pyfaulttolerance"
version = "0.0.2"
description = "Lightweight fault tolerance primitives for your modern Python microservices"
authors = [{ name = "Roman Hlushko", email = "roman.glushko.m@gmail.com" }]
license = { text = "Apache-2.0" }
readme = "README.md"
requires-python = ">=3.9,<4.0"

keywords = [
    "resiliency",
    "stability",
    "retries",
    "bulkhead",
    "rate limiting",
    "concurrency limiting",
    "microservices",
    "distributed systems",
    "timeouts",
    "circuit breakers"
]

classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Natural Language :: English",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: Implementation :: Stackless",
    "Topic :: Software Development",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: System :: Networking",
]

[project.urls]
Homepage = "https://pyfaulttolerance.readthedocs.io/en/latest/"
Repository = "https://github.com/roma-glushko/pyfaulttolerance"
Documentation = "https://pyfaulttolerance.readthedocs.io/en/latest/"

[project.optional-dependencies]
otel = [
    "opentelemetry-api>=1.41.1",
]
statsd = [
    "statsd>=4.0.1",
]
prometheus = [
    "prometheus-client>=0.25.0",
]

[dependency-groups]
dev = [
    "opentelemetry-api>=1.41.1",
    "statsd>=4.0.1",
    "prometheus-client>=0.25.0",
    "pytest>=8.4.2",
    "mypy>=1.19.1",
    "ruff>=0.15.12",
    "pytest-asyncio>=1.2.0",
    "coverage>=7.10.7",
    "mutmut>=3.3.1",
    "pytest-cov>=7.1.0",
    "opentelemetry-sdk>=1.41.1",
]
docs = [
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.7.6",
    "mdx-include>=1.4.2",
    "mkautodoc>=0.2.0",
    "httpx>=0.28.1",
    "fastapi>=0.128.8",
]

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".hg",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "venv",
]

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "C",  # flake8-comprehensions
    "B",  # flake8-bugbear
]
# Allow unused variables when underscore-prefixed.
dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"

[tool.ruff.lint.mccabe]
max-complexity = 10

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
